viber:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', env_var('DBT_PROJECT_DIR', env_var('DBT_PROFILES_DIR', '.')) ~ '/../warehouse.duckdb') }}"
      schema: main
      threads: 4
